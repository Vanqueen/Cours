import java.util.ArraList;
import java.util.List;

//importation de l'interface List

/**
 * add(E element) : ajoute un élément à la fin de la liste
 * add(int index) : ajoute un élément 
 * get(int index) : récupère l'élément à l'index spécifié
 * set(int index) : remplace l'élément à l'index spécifié
 * remove(Object o) : supprime la première occurrence de l'élément spécifié
 * remove(int index) : supprime l'élément à l'index spécifié
 * indexOf(Object o) : retourne l'index de la première occurrence de l'élément spécifié
 * lastIndexOf(Object o) : retourne l'index de la dernière occurrence de l'élément spécifié
 * contain(Object o) : vérifie si la liste contient l'élément spécifié
 * size(): retourne le nombre d'élément dans la liste
 */

// class ArrayList implements List
public class MyList {
    public static void main(String[] args) {
        // Création d'un ArrayList
        ArrayList<String> alphabet = new ArrayList<>();
        List<Integer> numbers = new ArrayList<>();

    }

}
